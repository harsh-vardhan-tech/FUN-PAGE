<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>I Love You 💘</title>
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins&display=swap" rel="stylesheet" />
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #ffb6c1, #ffd1dc);
      overflow: hidden;
    }

    h1, p {
      text-align: center;
      color: #fff;
    }

    .container {
      position: relative;
      text-align: center;
      padding: 50px 20px;
    }

    #nameInput {
      padding: 10px;
      font-size: 16px;
      margin: 10px;
    }

    #submitName {
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
    }

    #msg {
      font-size: 20px;
      color: #fff;
      margin: 20px 0;
    }

    button {
      padding: 15px 30px;
      font-size: 20px;
      border: none;
      border-radius: 30px;
      margin: 10px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    #loveBtn {
      background-color: #ff69b4;
      color: white;
    }

    #hateBtn {
      background-color: #555;
      color: white;
      position: absolute;
    }

    .gif {
      max-width: 200px;
      margin: 20px auto;
    }

    #namesList {
      background: rgba(255,255,255,0.2);
      padding: 10px;
      border-radius: 10px;
      margin-top: 20px;
      color: white;
    }

    footer {
      text-align: center;
      margin-top: 40px;
      color: white;
    }

    footer a {
      color: white;
      text-decoration: underline;
    }

    .heart {
      position: absolute;
      width: 20px;
      height: 20px;
      background: red;
      clip-path: polygon(50% 0%, 100% 35%, 80% 100%, 50% 80%, 20% 100%, 0% 35%);
      animation: float 4s infinite;
      opacity: 0.6;
    }

    @keyframes float {
      0% {
        transform: translateY(0) scale(1);
        opacity: 0.6;
      }
      100% {
        transform: translateY(-800px) scale(0.5);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <audio autoplay loop>
    <source src="https://files.catbox.moe/8xj46j.mp3" type="audio/mp3" />
  </audio>

  <div class="container">
    <h1>I Love You 💘</h1>
    <input type="text" id="nameInput" placeholder="What's your name baby? 😍" />
    <button id="submitName">Submit</button>

    <p id="msg">Do you love me? 😘</p>
    <button id="loveBtn">I Love You ❤️</button>
    <button id="hateBtn">I Hate You 💔</button>

    <div id="gifContainer" class="gif"></div>

    <div id="namesList"></div>
  </div>

  <footer>
    Made with ❤️ by <a href="https://instagram.com/chilgoja2.0" target="_blank">@chilgoja2.0</a>
  </footer>

  <!-- Floating Hearts -->
  <script>
    for (let i = 0; i < 50; i++) {
      let heart = document.createElement('div');
      heart.classList.add('heart');
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = (Math.random() * 3 + 2) + "s";
      document.body.appendChild(heart);
    }
  </script>

  <!-- Firebase CDN + Script -->
  <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js";
    import { getDatabase, ref, push, onValue } from "https://www.gstatic.com/firebasejs/12.2.1/firebase-database.js";

    const firebaseConfig = {
      apiKey: "AIzaSyBnwllZvZv2SXiB-__GxP8Y6D6KZx240xo",
      authDomain: "chilgoja-love-app.firebaseapp.com",
      databaseURL: "https://chilgoja-love-app-default-rtdb.firebaseio.com",
      projectId: "chilgoja-love-app",
      storageBucket: "chilgoja-love-app.appspot.com",
      messagingSenderId: "341233740750",
      appId: "1:341233740750:web:31ffece3aee37787dfd99f",
      measurementId: "G-PPMY6D9H35"
    };

    const app = initializeApp(firebaseConfig);
    const db = getDatabase(app);
    const namesRef = ref(db, "names");

    document.getElementById("submitName").onclick = () => {
      let name = document.getElementById("nameInput").value;
      if (name.trim()) {
        push(namesRef, name);
        document.getElementById("nameInput").style.display = "none";
        document.getElementById("submitName").style.display = "none";
      }
    };

    onValue(namesRef, (snapshot) => {
      let data = snapshot.val();
      let list = "<h3>LeaderBoard 💕</h3><ul>";
      for (let key in data) {
        list += `<li>${data[key]}</li>`;
      }
      list += "</ul>";
      document.getElementById("namesList").innerHTML = list;
    });

    const loveBtn = document.getElementById("loveBtn");
    const hateBtn = document.getElementById("hateBtn");
    const msg = document.getElementById("msg");
    const gifContainer = document.getElementById("gifContainer");

    const loveMessages = [
      "Uffff 😳 ye meri ada aayi re! 🙈🙈🙈🙈",
      "ILU ILU ILU 💌💞💓💗",
      "Pat gayiii oye hoyeeeee 😍",
      "Meri to life set ho gayi ji 🥹💘",
      "Omg tumne love dabaya 🥰 Main marr gayi 😭"
    ];

    const hateMessages = [
      "Awwww babu maan jao naa 🥺🥺",
      "Ufffff baby man jaaooo naa jaanuuu 😩💘",
      "Gadhu mere 😠 haan keh doooo 😭",
      "Uff yeh teri aadatein 😮‍💨 jaan haan keh do naa sooonuuuu 🐒",
      "Ale ale kya hua meri chutki ko button nahi dab raha kya 😂"
    ];

    const loveGIFs = [
      "https://tenor.com/bOCGGNvr0e4.gif",
      "https://tenor.com/rlHEGJNWyXQ.gif",
      "https://tenor.com/grHCh2kykkA.gif",
      "https://tenor.com/bgQ2r0EVp9x.gif",
      "https://tenor.com/lZv2vXGjzyR.gif"
    ];

    const finalGIFs = [
      "https://tenor.com/vHpMp7AeG5J.gif",
      "https://tenor.com/eWwvXV0Ipue.gif",
      "https://tenor.com/hK31jnfA8b8.gif"
    ];

    let hateClickCount = 0;

    loveBtn.onclick = () => {
      const randMsg = loveMessages[Math.floor(Math.random() * loveMessages.length)];
      const randGIF = loveGIFs[Math.floor(Math.random() * loveGIFs.length)];
      msg.innerHTML = randMsg;
      gifContainer.innerHTML = `<img src="${randGIF}" width="200" />`;
      loveBtn.style.transform = "scale(1.2)";
      setTimeout(() => loveBtn.style.transform = "scale(1)", 500);
    };

    hateBtn.onclick = () => {
      hateClickCount++;
      const randMsg = hateMessages[Math.floor(Math.random() * hateMessages.length)];
      msg.innerHTML = randMsg;
      gifContainer.innerHTML = "";

      // Move the button randomly
      hateBtn.style.top = Math.random() * 80 + "vh";
      hateBtn.style.left = Math.random() * 80 + "vw";

      // Hide after 8 clicks
      if (hateClickCount >= 8) {
        hateBtn.style.display = "none";
        const finalGIF = finalGIFs[Math.floor(Math.random() * finalGIFs.length)];
        gifContainer.innerHTML = `<img src="${finalGIF}" width="200" />`;
        msg.innerHTML = "Awww you finally said I LOVE YOU TOO! 💋💍";
      }
    };
  </script>
</body>
</html>
